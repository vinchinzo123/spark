Be sure to be on your own branch run the ~~git branch~~ command to ensure you are not on master/main

Be sure to ~~pipenv install~~ the needed dependencies 

Be sure to have a .env file with the correct SECRET_KEY